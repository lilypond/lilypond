\version "2.21.1"

\header {
  texidoc = "Multiple post events can be grouped into a single
post-event-like expression that dissolves into its constituents
as soon as it becomes attached to a music expression.
When property modifiers (such as tweaks or direction) or other
are applied to those, they are transfered to the contained
elements rather than being ignored."
}

zong=-!->

{ c'2^\zong c'_\zong }

