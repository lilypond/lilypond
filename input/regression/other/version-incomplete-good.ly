\header {
  texidoc = "An incomplete version statement, with the third number omitted,
is accepted if the second number is even.  This also tests the message
suggesting @command{convert-ly} in such cases."
}

expect-error = ##t

\version "2.22"
\nonexistentFunction
